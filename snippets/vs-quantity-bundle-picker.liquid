{% comment %}
  VentiSuisse quantity-bundle picker.

  Renders two pill buttons that drive the product form's quantity input:
    1 bottle (qty=1) | 2-pack — save N% (qty=2)

  The N% discount itself is applied as a Shopify Automatic Discount
  (Admin → Discounts → Automatic → Buy 2 of [VentiSuisse Bottle] get N% off).
  This widget only sets quantity; checkout math is server-side.

  Accepts:
  - product:          {Object} the current product (required)
  - discount_percent: {Number} discount % for 2-pack (optional, default 15)
  - bundle_url:       {String} optional URL to a curated 2-color bundle product
  - product_form_id:  {String} id of the product form (optional, falls back via DOM lookup)
  - section_id:       {String} id of the parent section (optional, for HTML anchoring)

  Usage:
    {% render 'vs-quantity-bundle-picker', product: product %}
    {% render 'vs-quantity-bundle-picker', product: product, discount_percent: 20, bundle_url: '/products/two-color-set' %}
{% endcomment %}

{%- liquid
  assign discount_pct = discount_percent | default: 15
  assign single_price_cents = product.selected_or_first_available_variant.price
  assign pack_multiplier = 100 | minus: discount_pct | times: 2
  assign pack_price_cents = single_price_cents | times: pack_multiplier | divided_by: 100
  assign single_money = single_price_cents | money
  assign pack_money = pack_price_cents | money
  assign discount_label = '−' | append: discount_pct | append: '%'
-%}

<vs-bundle-picker
  class="vs-bundle-picker-wrapper"
  data-section-id="{{ section_id }}"
  data-form-id="{{ product_form_id }}"
>
  <div
    class="vs-bundle-picker"
    role="radiogroup"
    aria-label="{{ 'products.product.quantity.label' | t | default: 'Quantity' }}"
  >
    <button
      type="button"
      class="vs-bundle-pill"
      data-qty="1"
      aria-pressed="true"
    >
      <span class="vs-bundle-pill__qty">1× {{ 'products.product.bundle.single' | t | default: 'bottle' }}</span>
      <span class="vs-bundle-pill__price">{{ single_money }}</span>
    </button>

    <button
      type="button"
      class="vs-bundle-pill"
      data-qty="2"
      aria-pressed="false"
    >
      <span class="vs-bundle-pill__qty">2× {{ 'products.product.bundle.pack' | t | default: 'pack' }}</span>
      <span class="vs-bundle-pill__price">{{ pack_money }}</span>
      <span class="vs-bundle-pill__save">{{ discount_label }}</span>
    </button>
  </div>

  {%- if bundle_url != blank -%}
    <a class="vs-bundle-picker__cross" href="{{ bundle_url }}">
      {{ 'products.product.bundle.curated_set' | t | default: 'Or get the curated Sky + Pink set →' }}
    </a>
  {%- endif -%}
</vs-bundle-picker>

<script>
  if (!customElements.get('vs-bundle-picker')) {
    customElements.define(
      'vs-bundle-picker',
      class extends HTMLElement {
        constructor() {
          super();
          this.pills = this.querySelectorAll('.vs-bundle-pill');
          this.formId = this.dataset.formId;
          this.pills.forEach((pill) => pill.addEventListener('click', this.onSelect.bind(this)));
        }

        onSelect(event) {
          const pill = event.currentTarget;
          const qty = parseInt(pill.dataset.qty, 10);

          this.pills.forEach((p) => p.setAttribute('aria-pressed', p === pill ? 'true' : 'false'));

          const form =
            (this.formId && document.getElementById(this.formId)) ||
            document.querySelector('form[data-type="add-to-cart-form"]');
          if (!form) return;
          const qtyInput = form.querySelector('input[name="quantity"]');
          if (!qtyInput) return;

          qtyInput.value = qty;
          qtyInput.dispatchEvent(new Event('change', { bubbles: true }));
        }
      }
    );
  }
</script>
